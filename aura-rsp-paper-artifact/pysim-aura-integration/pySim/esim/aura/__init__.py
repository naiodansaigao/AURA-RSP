"""AURA-RSP Profile Download research extension for pySim/osmo-smdpp.

The package is imported only when ``--rsp-mode aura`` is selected.  Standard
RSP therefore keeps its original dependency and execution path.
"""

AURA_PROTOCOL_VERSION = "AURA-RSP-v14-pysim-1"
